import { View, Text, Button, HStack, VStack, Center } from "native-base";

export default function Memo() {
  return (
    <Center>
      <Button
        alignItems='center'
        colorScheme={"darkBlue"}
      >
        ログをコピーする
      </Button>
    </Center>
  )
}
